/** Bottom offset for empire FABs (Build / Cancel). */
export const EMPIRE_BOTTOM_UI = 'calc(env(safe-area-inset-bottom, 0px) + 1.25rem)';

/** Raised during tutorial so the Build button stays above the home indicator. */
export const EMPIRE_BOTTOM_UI_TUTORIAL = 'calc(env(safe-area-inset-bottom, 0px) + 4.5rem)';
