/** Shared light-theme classes for Phil's Friends reels */
export const reelPageBg =
  'bg-gradient-to-b from-green-50 via-emerald-50/80 to-white';

export const reelFeedLoading = 'flex items-center justify-center bg-green-50';

/** Outer snap slide — centers the viewport */
export const reelSlideOuter =
  'w-full shrink-0 snap-start snap-always flex items-center justify-center min-h-0';

export const reelSlideActive = 'opacity-100';
export const reelSlideInactive = 'opacity-95';
